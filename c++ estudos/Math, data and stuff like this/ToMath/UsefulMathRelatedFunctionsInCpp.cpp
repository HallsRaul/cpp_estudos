#include <iostream>
#include <cmath>

int main()
{
    
    double x = 3.14;
    double y = 4;
    double z;

    //z = std::max(x,y);
    //z = std::min(x,y);
    //z = pow(2, 3); -> The result of raising base to the power exponent. -> O resultado de elevar a base ao expoente da potência.
    //z = sqrt(9); -> raiz quadrada.
    //z = abs(-3);  -> absolute value.
    //z = round(x);
    //z = ceil(x);
    //z = floor(x);

    std::cout << z;


    return 0;
}